{
  "name": "hearing-check-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:capacitor": "tsc && vite build --mode capacitor",
    "preview": "vite preview",
    "cap:sync": "npm run build:capacitor && npx cap sync"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.1",
    "@capacitor/share": "^8.0.1",
    "jspdf": "^4.2.1"
  }
}
