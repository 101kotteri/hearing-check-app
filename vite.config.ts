import { defineConfig } from 'vite';

// Served from https://101kotteri.github.io/hearing-check-app/ via GitHub Pages,
// so asset URLs need the repo name as a base path.
export default defineConfig({
  base: '/hearing-check-app/',
});
